local __util = require 'larian.util'

function MindEyeOveredgeChecker(entity)
    local entity = entity or context.Source
    if HasStatus('REINFORCEMENT_OVEREDGE', entity) then
        return SavingThrow(Ability.Intelligence, 12)
    else
        return SavingThrow(Ability.Intelligence, 18)
    end

end